# Optional: set when deploying frontend and backend separately (e.g. VITE_API_URL=https://api.yoursite.com)
# Leave unset for local dev; Vite proxies /api to the backend.
# VITE_API_URL=

# Exact Bid Tools screenshot extract (server only — never put this in VITE_*)
# OPENAI_API_KEY=sk-...
# Optional override (default gpt-4o):
# OPENAI_VISION_MODEL=gpt-4o
